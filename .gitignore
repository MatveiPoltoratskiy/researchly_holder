# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# scripts/enrich-next-opening.mjs's search cache and per-run candidate reports — local
# working files, never something to commit (the cache in particular is just an API-cost
# saver, not data anyone else needs)
scripts/.cache
scripts/.output

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
